class ExerciseGenerator:
    """
    Implements practice functionality for learning Morse code.

    Methods:
        generate_exercise(level: str) -> str
            Generates a practice exercise based on difficulty level.
    """

    def generate_exercise(self, level: str) -> str:
        """
        Generates a practice exercise based on difficulty level.

        Parameters:
            level (str): The difficulty level for the exercise.

        Returns:
            str: The generated exercise.

        Raises:
            ValueError: If the difficulty level is invalid.

        Example:
            >>> generator = ExerciseGenerator()
            >>> generator.generate_exercise("beginner")
            '... --- ...'
        """
        pass
